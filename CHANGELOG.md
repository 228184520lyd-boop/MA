# 4.0.133 — 更新后旧脚本URL复用

- manifest 的脚本入口加入当前版本查询参数，物理文件仍为根目录index.js。
- 构建检查入口URL版本，防止以后升级只更新标题而复用旧请求地址。
- 保留132的错误原因与当前任务状态修复；模型请求、总结、隐藏及恢复逻辑不变。
- 记录用户已安装132最新提交这一现场事实，不再将旧面板直接归因于下载或更新失败。
- README运行顺序修正为与源码一致：自动总结位于本轮提取写入之前。

# 4.0.132 — 总结失败误显完成与错误详情

- 修复下一轮任务沿用上一轮 lastResult、自动总结期间误显“已完成”。
- 增加总结阶段；修正“写入本轮提取”被识别为提取阶段的问题。
- 运行中仅当前阶段显示处理中，其余待确认；失败时保留失败步骤。
- 手动与自动总结记录是否实际执行，无待总结条目时显示跳过。
- 展开宿主 Error.cause 中的真实原因；弹窗显示阶段与原因，完整错误保留控制台。
- 继续使用官方 Connection Manager 调用合同，不增加请求重试，不改变总结和提取顺序。
- 延续无文件夹平铺部署包；源码保留开发目录。
- 补充原始126与131/132的小总结代码及固定输入对比，明确未解决的现场请求原因与验证范围。

# 4.0.131 平铺包装修订

- 部署包取消所有目录层级，manifest.json、完整 index.js 和 style.css 直接位于 ZIP 根目录。
- 源码包取消外层版本目录，保留 src/scripts/tests/docs 的职责分层。
- 打包前移除旧同名 ZIP，防止旧目录条目混入重建结果。
- 安装说明明确覆盖实际启用目录，关闭解压器自动新建同名文件夹。
- 本次不修改运行代码和样式，版本仍为 4.0.131。

# 4.0.131 — 发布加载断点与楼层布局

- 查明远程 4.0.130 上传目录被展平：index.js 导入 dist/app.js，但 bundle 实际位于根目录 app.js，导致模块无法加载。
- 改为根目录 index.js 包含完整运行代码；源码继续模块化。同步重定位构建后的宿主动态导入。
- 聊天楼层设置移到维护页最下方直接显示，移除运行页顶部设置及跳转按钮。
- 保留小总结隐藏、保留数量、恢复、简短状态提示和 .gitignore；补充完整目录提交说明。

# 4.0.130 — 楼层操作入口与提示

- 运行页顶部增加聊天楼层区：隐藏开关、带标签的保留数、显式保存按钮、小总结与恢复隐藏楼层按钮。
- 维护页增加直达入口；大总结阈值补充可见标签。
- 面板顶部显示版本号，方便确认加载版本。
- 聊天内“已记录 N 项变化”改为“已更新”，空闲提示简化为“就绪”；错误详情保留在面板和悬浮提示中。
- 补充 .gitignore 常见忽略项，并将其收录到部署包；dist/ 继续跟踪。

# 4.0.129 — 小总结隐藏与保留数量

- 取消大总结触发隐藏及对应开关；旧设置不再参与运行。
- 增加“保留最近楼层数”，默认10、最少1，按玩家/AI单条消息计数。
- 保留范围外也只隐藏已总结且有匹配来源的消息。
- 修改保留数即时调整已有隐藏范围，增加保留数时恢复相应消息。
- 更新大总结不隐藏回归，新增默认值、奇数保留、数量修改、系统消息排除等检查。

# 4.0.128 — 总结隐藏功能候选版

- 提取完成后记录对应玩家/AI 消息的身份，不复制正文。
- 小总结、大总结成功写入后按完整场景覆盖范围设置 ST 原生隐藏标记。
- 添加独立小/大总结隐藏开关及恢复按钮，保留最新一轮。
- 接入总结撤销、回执恢复、Edit/Swipe/Delete、刷新、清空和重置链。
- 镜渊隐藏不改变消息身份；隐藏来源可以参与原有提取重建。
- 隐藏写入回读核验失败单独提示，并保留已提交总结的撤销入口。
- 手动总结期间编辑或删除消息会取消陈旧任务；恢复相关操作共用现有任务生命周期。
- 新增 20 个集成验证场景，源码与部署产物运行同套回归。

# 4.0.127 — 结构重构候选版

- 按职责拆分核心源码并统一格式。
- 构建产物移至 dist/，回归检查移至 tests/。
- 源码包改用明确收录清单，保留 .gitignore，阻止临时文件混入。
- 同步构建入口与宿主相对路径。
- 修复构建器缩进污染多行字符串；增加部署产物同套回归。
- 保留现有功能、提示词与数据格式；本版未新增隐藏楼层。

# Changelog

## 4.0.126

- 整本世界书整理取消每批重复发送全书 `globalIndex`；每个请求只携带当前 batch 的完整条目。
- 整本整理改为使用 SillyTavern token counter 组批，默认输入目标 6000 tokens；单条输入超过 12000 tokens 时直接停止并提示，不拖累整本任务。
- 502/503/504、429、网络超时等 transport failure 不再触发同尺寸 plain-JSON 重试；多条 batch 会机械二分后继续，单条仍失败则停止。
- Connection Manager Structured Output 改为 `extractData:false`：ST 继续负责 Profile/provider 路由和 `json_schema`，镜渊拿到原始 provider 响应后自行机械解析，避免宿主提前把损坏 JSON 吞成空对象。
- JSON 外层解析兼容代码围栏、前后说明、非法控制字符、注释和尾逗号；`complete=true`、E 数量、E 身份、重复/缺失检查继续严格，任何不完整结果零提交。
- 小总结提示词收回为“根据以上材料进行总结、合并与提炼”；大总结只增加“更高层级”。不再由插件提示词规定长期、重点、过程、阶段性等语义标准。
- 仓库发布化整理：用户 README 精简；历史开发 README、Host Research 归档到 `docs/archive/`；新增 CHANGELOG、Troubleshooting、Development 与 Host Contracts。

## 4.0.125

- 场景生命周期只读取本轮新提取的正常 `场景` 条目；ST 激活的旧场景条目不参与切场。
- 同一连续场景可以不重复输出场景条目；首次场景和真正切场必须产生稳定的场景事实。
- 保留空 SceneGroup 的零模型机械收尾。

## 4.0.124

- 修复空 SceneGroup 永久 `closed && !small` 重试问题。
- 曾实验每轮 `[当前场景]` 控制行；该设计在 4.0.125 收回。

## 4.0.123

- 整本整理改为独立 JSON 合同；E 身份与完整性严格校验；预览后 fresh-read 防止覆盖玩家改动。

## 4.0.122

- Edit / Swipe / Delete 历史补建改为跟随当前 ST `world_info_depth` 的有限窗口；范围外较新状态受保护。

## 4.0.121

- 辅助提取输入增加 16000-token 硬上限；优先使用 ST token counter，不修改 ST World Info Budget。

## 4.0.120

- 审核/润色收敛为单一“终稿处理”；正常路径一调用，只有协议损坏允许一次纠错。
- 提取参考收敛为全局薄身份目录 + ST 本轮实际 `WORLD_INFO_ACTIVATED` 详情。
- 普通世界书保存不再隐式全局重排。

## 4.0.119 及更早

完整历史开发记录保存在 `docs/archive/legacy/README-4.0.125.md`。
